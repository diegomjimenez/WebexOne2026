# Copy this file to .env and fill in your values. Never commit real tokens.

# Your Webex bot token (create at https://developer.webex.com/my-apps).
BOT_TOKEN=

# Approved email domain for the bot (messages from other domains are ignored).
DOMAIN=

# Your OpenAI API key (used by the MCP client and the bot).
OPENAI_API_KEY=

# ── MCP server connection (REQUIRED) ────────────────────────────────────────
# This bot is MCP-server-agnostic: it connects to whatever server(s) you set.
#
# MULTI-SERVER (recommended for the full lab experience):
# Use indexed suffixes (_1, _2, …) to connect to multiple servers at once.
# Tools, resources, and prompts from all servers are merged automatically.
MCP_SERVER_COMMAND=python

MCP_SERVER_ARGS_1=06_manage_address_books.py
MCP_SERVER_CWD_1=../mcp_servers

MCP_SERVER_ARGS_2=07_verify_desktop_profiles.py
MCP_SERVER_CWD_2=../mcp_servers

# SINGLE-SERVER (fallback — used only when no _1/_2/… vars are set):
# MCP_SERVER_ARGS=07_verify_desktop_profiles.py
# MCP_SERVER_CWD=../mcp_servers

# ── Model (REQUIRED) ────────────────────────────────────────────────────────
# Set to any model your OpenAI project (or compatible provider) has access to.
# The bot does NOT hardcode a model — you choose what to run.

MODEL=gpt-5-nano

# ── System prompt (optional) ────────────────────────────────────────────────
# Base persona is loaded from system_prompt.txt next to the script by default.
# Override with an absolute or relative path to reuse the bot for another domain.
# SYSTEM_PROMPT_FILE=./system_prompt.txt

# ── Configurable slash command (optional) ───────────────────────────────────
# Triggers one of the connected server's prompts as an explicit "/keyword".
# Leave SETUP_PROMPT_NAME empty to disable the command.
# SETUP_COMMAND_KEYWORD=setup
# SETUP_PROMPT_NAME=
# SETUP_PROMPT_ARGS={}

# ── Skills directory (optional) ─────────────────────────────────────────────
# Defaults to ./skills relative to the script.
SKILLS_DIR=./skills